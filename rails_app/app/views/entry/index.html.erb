<h1>Entries</h1>

 <% @entries.each do |e| %>
     <div className="entry">
         <h2><%= e.name %></h2>
         <p><%= e.entry %></p>
         <p><%= link_to "Link", entry_path(id: e.id)%></p>
     </div>
 <% end %>

